% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lv_window_contacts}
\alias{lv_window_contacts}
\title{Total contacts with at least one endpoint inside a boundary window}
\usage{
lv_window_contacts(bin1, bin2, count, bin_start, bin_size, flank)
}
\arguments{
\item{bin1, bin2}{integer vectors of pixel endpoint bins}

\item{count}{pixel counts}

\item{bin_start}{genomic start (bp) of the boundary bin}

\item{bin_size}{bin resolution (bp)}

\item{flank}{window extension on both sides (bp)}
}
\value{
total contact count touching the window
}
\description{
The window spans [bin_start - flank, bin_start + bin_size + flank);
bins are 1-based, positions derived as (bin - 1) * bin_size.
}
\examples{
lv_window_contacts(c(1L, 4L, 9L), c(2L, 5L, 10L), c(2, 3, 4),
                   bin_start = 4000, bin_size = 1000, flank = 1500)
}
