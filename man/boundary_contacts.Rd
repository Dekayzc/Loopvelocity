% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{boundary_contacts}
\alias{boundary_contacts}
\title{Per-cell contacts around TAD boundaries}
\usage{
boundary_contacts(cell_contacts, boundaries, flank = 25000)
}
\arguments{
\item{cell_contacts}{named list of \code{lv_contacts} (one per cell, same
bin grid)}

\item{boundaries}{data.frame with columns chrom, start, end on that grid}

\item{flank}{window extension in bp (default 25 kb -> ~50 kb windows)}
}
\value{
TAD x cell matrix of window contact counts
}
\description{
For each cell and each boundary, sums contact pixels with at least one
endpoint inside the boundary window [start - flank, end + flank).
}
\examples{
bins <- data.frame(chrom = "chr1", start = c(0, 1e4, 2e4), end = c(1e4, 2e4, 3e4))
px <- data.frame(bin1_id = c(1L, 1L, 2L), bin2_id = c(2L, 3L, 3L), count = c(2, 1, 4))
cells <- list(cA = structure(list(bins = bins, pixels = px), class = "lv_contacts"))
bnd <- data.frame(chrom = "chr1", start = 1e4, end = 2e4)
boundary_contacts(cells, bnd, flank = 5000)
}
