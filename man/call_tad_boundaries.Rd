% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{call_tad_boundaries}
\alias{call_tad_boundaries}
\title{Call TAD boundaries from an insulation profile}
\usage{
call_tad_boundaries(m, window, min_delta = 0.2)
}
\arguments{
\item{m}{square dense contact matrix}

\item{window}{diamond radius for insulation}

\item{min_delta}{lower-quantile cutoff on the insulation dip}
}
\value{
data.frame with columns bin, insulation, delta
}
\description{
A bin is a boundary when its insulation score is a local minimum and the
dip relative to the flanking minima (the delta) falls below the
\code{min_delta} quantile of the delta distribution.
}
\examples{
n <- 40; m <- matrix(0, n, n)
idx <- expand.grid(i = 1:n, j = 1:n)
same <- (idx$i <= 20) == (idx$j <= 20)
m[as.matrix(idx[same & idx$i != idx$j, ])] <- 1
call_tad_boundaries(m, window = 3, min_delta = 0.1)
}
