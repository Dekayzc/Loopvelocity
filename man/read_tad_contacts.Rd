% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{read_tad_contacts}
\alias{read_tad_contacts}
\title{Read boundary contact tables into S/I matrices}
\usage{
read_tad_contacts(
  left_bed,
  right_bed,
  assign = TRUE,
  test = c("t", "wilcox"),
  padj = 0.05
)
}
\arguments{
\item{left_bed, right_bed}{paths to the two boundary tables}

\item{assign}{run S/I assignment (default) or return raw left/right}

\item{test, padj}{passed to \link{assign_si_boundaries}}
}
\value{
\code{lv_si} list, or \code{list(left, right)} when assign = FALSE
}
\description{
Quick path for pre-summarized boundary tables (chr, start, end,
contacts, cell, regionid): pivots them to TAD x cell matrices and runs
\link{assign_si_boundaries}.
}
\examples{
fl <- tempfile(); fr <- tempfile()
writeLines(c("chr1\t50000\t55000\t10\tcellA\t1",
            "chr1\t50000\t55000\t12\tcellB\t1"), fl)
writeLines(c("chr1\t120000\t125000\t2\tcellA\t1",
            "chr1\t120000\t125000\t1\tcellB\t1"), fr)
si <- read_tad_contacts(fl, fr)   # left/right pair by chr + region id
si$S[, c("cellA", "cellB")]
}
