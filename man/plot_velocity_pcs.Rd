% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{plot_velocity_pcs}
\alias{plot_velocity_pcs}
\title{Project velocity onto principal components}
\usage{
plot_velocity_pcs(
  vel,
  nPcs = 4,
  cell.colors = NULL,
  scale = "log",
  plot.cols = min(3, nPcs - 1),
  norm.nPcs = NA,
  show.grid.flow = FALSE,
  grid.n = 20,
  grid.sd = NULL,
  min.grid.cell.mass = 1,
  min.arrow.size = NULL,
  arrow.scale = 1,
  arrow.lwd = 1,
  fixed.arrow.length = FALSE,
  plot.grid.points = FALSE,
  max.grid.arrow.length = NULL,
  size.norm = FALSE,
  do.par = TRUE,
  n.cores = default_n_cores(),
  ...
)
}
\arguments{
\item{vel}{velocity result}

\item{nPcs}{number of successive PCs to visualize}

\item{cell.colors}{named colour vector}

\item{scale}{contact transform ("log" default)}

\item{plot.cols}{layout columns}

\item{norm.nPcs}{PCs for magnitude normalization (reserved)}

\item{show.grid.flow, grid.n, grid.sd, min.grid.cell.mass, min.arrow.size}{grid-flow controls as in \link{plot_loop_velocity}}

\item{arrow.scale}{displacement scale multiplier}

\item{arrow.lwd}{arrow line width}

\item{fixed.arrow.length, plot.grid.points, max.grid.arrow.length}{further grid rendering options}

\item{size.norm}{rescale current/projected by cell totals}

\item{do.par}{reset graphics parameters}

\item{n.cores}{worker threads}

\item{...}{passed to \code{plot()}}
}
\value{
invisibly, PCA info and projected velocity PCs
}
\description{
Diagnostic plot: cells in consecutive PC pairs of the current contact
state, with arrows along the projected velocity increments.
}
\examples{
set.seed(1); n <- 25
th <- 0.7; Ig <- runif(n, 0.5, 3)
S <- matrix((1 - th) / th * Ig + 2, 3, n,
           dimnames = list(paste0("tad", 1:3), paste0("c", 1:n)))
I <- matrix(Ig, 3, n, dimnames = dimnames(S))
vel <- loop_velocity_estimates(S, I, mode = "two-sided", kCells = 1,
                               mult = 1e9, verbose = FALSE)
cc <- setNames(rep("grey60", n), colnames(S))
plot_velocity_pcs(vel, nPcs = 2, cell.colors = cc)
}
